# Untrack this file from git by running
# git update-index --assume-unchanged settings.nix
# or by running setup.sh
# Then overwrite settings defined in global or per-device settings-defaults.nix

{ config, pkgs, lib, ... }:

{
  config = {
    
  };
}
