# Untrack this file from git by running
# git update-index --assume-unchanged credentials.nix
# Then overwrite credentials defined in credentials-template.nix

{ config, pkgs, lib, ... }:

{
  config = {
    
  };
}
